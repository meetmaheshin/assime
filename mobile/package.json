{
  "name": "aarth-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "AARTH native shell (Capacitor) — wraps the web app and unlocks on-device notifications.",
  "scripts": {
    "sync": "cap sync",
    "apk": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@capacitor/local-notifications": "^8.2.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}
